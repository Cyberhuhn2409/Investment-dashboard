import { NotFoundState } from "@/components/states";

export default function NotFound() {
  return (
    <NotFoundState
      title="Seite nicht gefunden"
      text="Diese Seite oder dieses Symbol gibt es nicht. Signal deckt über 500 Werte ab – US-Aktien aller Größen sowie DAX, MDAX und SDAX. Die Suche hilft weiter."
    />
  );
}
