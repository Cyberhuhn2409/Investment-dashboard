import { NotFoundState } from "@/components/states";

export default function NotFound() {
  return (
    <NotFoundState
      title="Seite nicht gefunden"
      text="Diese Seite oder dieses Symbol gibt es nicht. Signal deckt ~150 US-Large-Caps und den DAX 40 ab – die Suche hilft weiter."
    />
  );
}
