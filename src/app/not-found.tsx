import { NotFoundState } from "@/components/states";

export default function NotFound() {
  return <NotFoundState title="Seite nicht gefunden" text="Diese Seite gibt es nicht (mehr). Vielleicht hilft die Suche weiter." />;
}
